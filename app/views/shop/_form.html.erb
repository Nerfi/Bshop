<div class="form-inputs">
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price %>
</div>

<div class="form-actions">
  <% if policy(Shop).create? %>
  <%= f.button :submit %>
  <% end %>

</div>
